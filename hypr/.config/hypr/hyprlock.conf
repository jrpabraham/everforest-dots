$color = rgba(45,53,59, 1.0)
$inner_color = rgba(45,53,59, 0.8)
$outer_color = rgba(211,198,170, 1.0)
$font_color = rgba(211,198,170, 1.0)
$check_color = rgba(127,187,179, 1.0)

general {
    ignore_empty_input = true
}

background {
    monitor =
    color = $color
    path = ~/Pictures/richard-tao-jlJFU9QBfFc-unsplash.jpg
    blur_passes = 2
}

animations {
    enabled = false
}

input-field {
    monitor =
    size = 200, 40
    position = 0, 0
    halign = center
    valign = center

    inner_color = $inner_color
    outer_color = $outer_color
    outline_thickness = 4

    font_family = JetBrainsMono Nerd Font
    font_color = $font_color

    placeholder_text = Authenticate
    check_color = $check_color
    fail_text = <i>$FAIL ($ATTEMPTS)</i>

    rounding = 0
    shadow_passes = 0
    fade_on_empty = false
}

auth {
    fingerprint:enabled = false
}
